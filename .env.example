# ============================================
# ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to version control.

# ── Site URL ─────────────────────────────────
# Used for SEO (sitemap, robots, canonical URLs, Open Graph).
# Set to your production domain (e.g. https://yourdomain.com).
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ── Supabase (Optional — needed for contact form & waitlist) ──
# Create a project at https://supabase.com and copy these values.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# ── Rate Limiting (Optional) ────────────────
# RATE_LIMIT_SECRET=your_secret_for_rate_limiting
